{% extends 'base.html.twig' %}

{% block title %}Mot de passe{% endblock %}

{% block body %}
<div class="row">
    
    {% include "settings/_sidebar.html.twig" %}

    <div class="mt-5 mb-5 pb-3 col-xs-12 col-md-10">
        
        <h1>changer le mot de passe</h1>
            {{ form_start(form) }}
            {{ form_end(form) }}
            
            {% for message in app.flashes ('modif') %}
            <div class="alert alert-success">
                {{ message }}
            </div>
            {% endfor %}
    </div>      
    

</div>
{% endblock %}